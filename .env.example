# Keep empty in development to use Vite proxy (/api -> VITE_API_PROXY_TARGET).
VITE_API_BASE_URL=
# Backend URL used by Vite dev proxy.
VITE_API_PROXY_TARGET=http://localhost:5000
# Auth API route prefix used by frontend (examples: /api/auth or /auth).
VITE_AUTH_API_BASE=/api/auth
